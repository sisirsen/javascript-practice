const user = {
  name: "Sisir",
  age: 21
};

const jsonData = JSON.stringify(user);

console.log(jsonData);

//The difference is in object {name:"sisir",age:21}.
                  //in Json {"name":"sisir","age":21}